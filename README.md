# chat-backend

after cloning, please try the following commands in your terminal:

go to directory using command:
cd chat-backend

install/update packages
npm install

run the server
nodemon index.js
